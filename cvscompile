#!/bin/bash

libtoolize --force --copy --automake
aclocal-1.7 $ACLOCAL_FLAGS
autoheader
automake-1.7 --foreign --copy --add-missing
autoconf
./configure --enable-maintainer-mode $@ || exit 1
if [ -z "$CVSCOMPILE_NO_MAKE" ]; then
  make
fi
